JavaScript Arrays — Top Coding Questions
Reverse an array without using the built-in reverse() method.

Find the maximum and minimum values in an array.

Remove duplicates from an array.

Merge two sorted arrays into a single sorted array.

Find the first non-repeating element in an array.

Check if two arrays are equal (same elements and order).

Implement map() or filter() functionality manually.

Flatten a nested array (e.g., [1, [2, [3, 4]]] → [1, 2, 3, 4]).

Find the pair of elements that sum up to a given target number.

Rotate an array to the right by k steps.

Implement array chunking: split an array into chunks of size n.

Find the intersection of two arrays.

Check if an array is a palindrome.

Count the frequency of elements in an array (return object/map).

Implement reduce() function manually.

Find the missing number in an array containing numbers from 1 to n.

Move all zeros in an array to the end while maintaining the order of other elements.

Find the longest increasing subarray or subsequence.

Find the contiguous subarray with the maximum sum (Kadane’s algorithm).

Group objects by a property in an array of objects.