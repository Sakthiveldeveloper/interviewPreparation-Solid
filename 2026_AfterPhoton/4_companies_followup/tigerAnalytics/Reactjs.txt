
### ✅ React  Prep Structure:

| # | Section | Purpose |
|---|---------|---------|
| 1 | 🗺️ **Mind Map** | Visual overview of all  topics |
| 2 | 📖 **Theory Questions** | Concept-based questions + answers |
| 3 | 💻 **Coding Questions** | Write actual code  |
| 4 | 🎯 **Scenario Questions** | Real-world problem solving (Senior/Lead level) |

----------------------------------------------------------------------------------------------
# ⚛️ REACT — COMPLETE DEEP DIVE

## 🗺️ REACT MIND MAP

    ```
    React
    │
    ├── 1. Core Fundamentals
    │   ├── What is React? Virtual DOM
    │   ├── JSX
    │   ├── Components (Functional vs Class)
    │   ├── Props & State
    │   └── Component Lifecycle
    │
    ├── 2. Hooks
    │   ├── useState
    │   ├── useEffect
    │   ├── useRef
    │   ├── useContext
    │   ├── useReducer
    │   ├── useMemo
    │   ├── useCallback
    │   └── Custom Hooks
    │
    ├── 3. State Management
    │   ├── Local State (useState)
    │   ├── Context API
    │   ├── Redux (actions, reducers, store)
    │   ├── Redux Toolkit
    │   └── When to use what
    │
    ├── 4. Performance Optimization
    │   ├── React.memo
    │   ├── useMemo & useCallback
    │   ├── Lazy Loading & Suspense
    │   ├── Code Splitting
    │   └── Virtual List / Windowing
    │
    ├── 5. Component Patterns
    │   ├── Higher Order Components (HOC)
    │   ├── Render Props
    │   ├── Compound Components
    │   ├── Controlled vs Uncontrolled
    │   └── Error Boundaries
    │
    ├── 6. React Router
    │   ├── BrowserRouter, Routes, Route
    │   ├── useNavigate, useParams, useLocation
    │   ├── Protected Routes
    │   └── Nested Routes
    │
    ├── 7. API Integration
    │   ├── fetch / axios
    │   ├── useEffect for data fetching
    │   ├── Loading & Error states
    │   ├── React Query / SWR
    │   └── Axios interceptors
    │
    └── 8. Advanced Concepts
        ├── Reconciliation & Diffing
        ├── Keys in lists
        ├── Portals
        ├── Refs & forwardRef
        └── React 18 features (Concurrent, Transitions)
    ```

-----------------------------------------------------------------------------------------------------

## 📖 THEORY QUESTIONS + ANSWERS

    ---

    **Q1. What is React? What is Virtual DOM?**

    React is a JavaScript library for building user interfaces using component-based architecture.

    **Virtual DOM:**
    - Virtual DOM is a lightweight JS object copy of the real DOM
    - When state changes, React creates a new Virtual DOM tree
    - React **diffs** the new vs old Virtual DOM (reconciliation)
    - Only the **changed parts** are updated in real DOM (not entire page!)

    ```
    State Change
        ↓
    New Virtual DOM created
        ↓
    Diff old vs new Virtual DOM
        ↓
    Only changed nodes updated in real DOM
        ↓
    Fast & efficient UI updates!
    ```

    **Benefits:** Much faster than directly manipulating real DOM every time!

    ---

    **Q2. JSX — What is it? How does it work?**

    JSX = JavaScript XML — allows writing HTML-like syntax inside JavaScript.

    ```jsx
    // JSX
    const element = <h1 className="title">Hello {name}</h1>;

    // Compiles to (what browser actually runs):
    const element = React.createElement(
    'h1',
    { className: 'title' },
    `Hello ${name}`
    );

    // JSX Rules:
    // 1. Must have ONE root element (or use Fragment)
    // 2. Use className instead of class
    // 3. Use htmlFor instead of for
    // 4. Self-close empty tags: <img />
    // 5. Expressions inside {}

    // Fragment — avoids extra div wrapper
    return (
    <>
        <h1>Title</h1>
        <p>Content</p>
    </>
    );
    ```

    ---

    **Q3. Functional vs Class Components?**

    ```jsx
    // Class Component (old way)
    class Welcome extends React.Component {
    constructor(props) {
        super(props);
        this.state = { count: 0 };
    }

    componentDidMount() { /* runs after mount */ }
    componentDidUpdate() { /* runs after update */ }
    componentWillUnmount() { /* cleanup */ }

    render() {
        return <h1>Hello {this.props.name}</h1>;
    }
    }

    // Functional Component (modern — preferred!)
    function Welcome({ name }) {
    const [count, setCount] = useState(0);

    useEffect(() => {
        // componentDidMount + componentDidUpdate
        return () => { /* componentWillUnmount */ };
    }, []);

    return <h1>Hello {name}</h1>;
    }

    // Why Functional is preferred:
    // 1. Simpler and cleaner code
    // 2. Hooks replace lifecycle methods
    // 3. Better performance
    // 4. Easier to test
    // 5. No 'this' confusion
    ```

    ---

    **Q4. Props vs State?**

    ```jsx
    // PROPS — data passed FROM parent TO child (read-only!)
    function UserCard({ name, age, onDelete }) {
    // Cannot modify props!
    return (
        <div>
        <h2>{name}</h2>
        <p>{age}</p>
        <button onClick={() => onDelete(name)}>Delete</button>
        </div>
    );
    }

    // Parent passes props
    <UserCard name="Sakthi" age={28} onDelete={handleDelete} />

    // STATE — data managed INSIDE component (mutable!)
    function Counter() {
    const [count, setCount] = useState(0);

    return (
        <div>
        <p>{count}</p>
        <button onClick={() => setCount(count + 1)}>+</button>
        </div>
    );
    }

    // Key differences:
    // Props: passed from parent, read-only, triggers re-render when changed
    // State: managed internally, mutable via setter, triggers re-render when changed
    ```

    ---

    **Q5. All React Hooks — When to use each?**

    ```jsx
    // useState — local state management
    const [value, setValue] = useState(initialValue);

    // useEffect — side effects (API calls, subscriptions, DOM manipulation)
    useEffect(() => {
    // runs after render
    return () => { /* cleanup */ };
    }, [dependencies]);

    // useRef — mutable value that doesn't trigger re-render / DOM reference
    const inputRef = useRef(null);
    inputRef.current.focus(); // access DOM element

    // useContext — consume context without prop drilling
    const theme = useContext(ThemeContext);

    // useReducer — complex state logic (alternative to useState)
    const [state, dispatch] = useReducer(reducer, initialState);

    // useMemo — memoize expensive calculations
    const expensiveValue = useMemo(() => heavyCalc(data), [data]);

    // useCallback — memoize functions to prevent child re-renders
    const handleClick = useCallback(() => doSomething(id), [id]);

    // useLayoutEffect — like useEffect but fires synchronously after DOM update
    useLayoutEffect(() => { /* measure DOM */ }, []);
    ```

    ---

    **Q6. useEffect — Deep Dive?**

    ```jsx
    // 1. Runs after EVERY render (no dependency array)
    useEffect(() => {
    console.log("Runs after every render");
    });

    // 2. Runs ONLY on mount (empty dependency array)
    useEffect(() => {
    console.log("Runs once on mount — like componentDidMount");
    fetchData();
    }, []);

    // 3. Runs when specific values change
    useEffect(() => {
    console.log("Runs when userId changes");
    fetchUser(userId);
    }, [userId]);

    // 4. Cleanup — runs before next effect OR on unmount
    useEffect(() => {
    const subscription = subscribe(userId);

    return () => {
        subscription.unsubscribe(); // cleanup!
    };
    }, [userId]);

    // 5. Common mistake — missing dependency
    const [count, setCount] = useState(0);

    useEffect(() => {
    document.title = `Count: ${count}`; // count used but not in deps!
    }, []); // ❌ Wrong — stale closure!

    useEffect(() => {
    document.title = `Count: ${count}`;
    }, [count]); // ✅ Correct!
    ```

    ---

    **Q7. Context API — How to use?**

    ```jsx
    // 1. Create context
    const ThemeContext = createContext('light');
    const UserContext = createContext(null);

    // 2. Provide context (wrap components)
    function App() {
    const [theme, setTheme] = useState('light');
    const [user, setUser] = useState({ name: "Sakthi" });

    return (
        <ThemeContext.Provider value={{ theme, setTheme }}>
        <UserContext.Provider value={{ user, setUser }}>
            <MainApp />
        </UserContext.Provider>
        </ThemeContext.Provider>
    );
    }

    // 3. Consume context (any nested component)
    function Header() {
    const { theme, setTheme } = useContext(ThemeContext);
    const { user } = useContext(UserContext);

    return (
        <header className={theme}>
        <span>{user.name}</span>
        <button onClick={() => setTheme(
            theme === 'light' ? 'dark' : 'light'
        )}>
            Toggle Theme
        </button>
        </header>
    );
    }

    // When to use Context:
    // ✅ Theme, language, authentication, user data
    // ❌ Frequently changing data (causes re-renders) — use Redux instead
    ```

    ---

    **Q8. Redux vs Context API — When to use?**

    ```jsx
    // CONTEXT API — simple global state
    // ✅ Use for: theme, user auth, language settings
    // ❌ Avoid for: large apps, frequent updates, complex logic

    // REDUX — predictable state container
    // ✅ Use for: large apps, complex state, frequent updates, dev tools needed

    // Redux flow:
    // UI dispatches Action → Reducer processes Action → Store updates → UI re-renders

    // Redux Toolkit (modern Redux — recommended!)
    import { createSlice, configureStore } from '@reduxjs/toolkit';

    const counterSlice = createSlice({
    name: 'counter',
    initialState: { value: 0 },
    reducers: {
        increment: state => { state.value += 1; },
        decrement: state => { state.value -= 1; },
        incrementByAmount: (state, action) => {
        state.value += action.payload;
        }
    }
    });

    export const { increment, decrement, incrementByAmount } = counterSlice.actions;

    const store = configureStore({
    reducer: { counter: counterSlice.reducer }
    });

    // In component
    function Counter() {
    const count = useSelector(state => state.counter.value);
    const dispatch = useDispatch();

    return (
        <div>
        <p>{count}</p>
        <button onClick={() => dispatch(increment())}>+</button>
        <button onClick={() => dispatch(decrement())}>-</button>
        </div>
    );
    }
    ```

    ---

    **Q9. Performance Optimization in React?**

    ```jsx
    // 1. React.memo — prevent child re-render if props unchanged
    const UserCard = React.memo(function UserCard({ name, age }) {
    console.log("UserCard rendered");
    return <div>{name} - {age}</div>;
    });

    // 2. useCallback — stable function reference for child props
    function Parent() {
    const [count, setCount] = useState(0);

    // Without useCallback — new function every render = child re-renders!
    // With useCallback — same function reference = child doesn't re-render!
    const handleClick = useCallback(() => {
        console.log("clicked");
    }, []); // no dependencies = created once

    return (
        <>
        <button onClick={() => setCount(c => c + 1)}>Count: {count}</button>
        <UserCard onClick={handleClick} /> {/* won't re-render! */}
        </>
    );
    }

    // 3. useMemo — expensive calculation
    function DataTable({ data, filter }) {
    // Without useMemo — recalculates on every render!
    // With useMemo — only recalculates when data or filter changes
    const filteredData = useMemo(() =>
        data.filter(item => item.name.includes(filter)),
        [data, filter]
    );

    return <table>{/* render filteredData */}</table>;
    }

    // 4. Lazy loading — load component only when needed
    const HeavyChart = lazy(() => import('./HeavyChart'));

    function Dashboard() {
    return (
        <Suspense fallback={<div>Loading...</div>}>
        <HeavyChart />
        </Suspense>
    );
    }

    // 5. Key prop — help React identify list items
    // ❌ Never use index as key for reorderable lists!
    items.map((item, index) => <Item key={index} />); // BAD!
    items.map(item => <Item key={item.id} />); // GOOD!
    ```

    ---

    **Q10. Error Boundaries, Portals, forwardRef?**

    ```jsx
    // ERROR BOUNDARIES — catch JS errors in component tree
    class ErrorBoundary extends React.Component {
    state = { hasError: false };

    static getDerivedStateFromError(error) {
        return { hasError: true };
    }

    componentDidCatch(error, errorInfo) {
        console.error('Error caught:', error, errorInfo);
    }

    render() {
        if (this.state.hasError) {
        return <h1>Something went wrong!</h1>;
        }
        return this.props.children;
    }
    }

    // Usage
    <ErrorBoundary>
    <MyComponent /> {/* if this crashes, ErrorBoundary catches it */}
    </ErrorBoundary>

    // PORTALS — render outside parent DOM node
    function Modal({ children }) {
    return ReactDOM.createPortal(
        <div className="modal">{children}</div>,
        document.getElementById('modal-root') // renders here!
    );
    }
    // Great for modals, tooltips, dropdowns

    // forwardRef — pass ref to child component
    const Input = forwardRef((props, ref) => {
    return <input ref={ref} {...props} />;
    });

    function Parent() {
    const inputRef = useRef(null);

    return (
        <>
        <Input ref={inputRef} placeholder="Type here" />
        <button onClick={() => inputRef.current.focus()}>
            Focus Input
        </button>
        </>
    );
    }
    ```

    ---

-----------------------------------------------------------------------------------------------------

## 💻 CODING QUESTIONS

    ---

    **C1. Build a custom useFetch hook:**
    ```jsx
    function useFetch(url) {
    const [data, setData] = useState(null);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);

    useEffect(() => {
        let isMounted = true;
        setLoading(true);

        fetch(url)
        .then(res => {
            if (!res.ok) throw new Error(`HTTP error! ${res.status}`);
            return res.json();
        })
        .then(data => {
            if (isMounted) {
            setData(data);
            setLoading(false);
            }
        })
        .catch(err => {
            if (isMounted) {
            setError(err.message);
            setLoading(false);
            }
        });

        return () => { isMounted = false; }; // cleanup!
    }, [url]);

    return { data, loading, error };
    }

    // Usage
    function UserProfile({ userId }) {
    const { data, loading, error } = useFetch(`/api/users/${userId}`);

    if (loading) return <Spinner />;
    if (error) return <Error message={error} />;
    return <div>{data.name}</div>;
    }
    ```

    ---

    **C2. Build a custom useLocalStorage hook:**
    ```jsx
    function useLocalStorage(key, initialValue) {
    const [value, setValue] = useState(() => {
        try {
        const stored = localStorage.getItem(key);
        return stored ? JSON.parse(stored) : initialValue;
        } catch {
        return initialValue;
        }
    });

    const setStoredValue = useCallback((newValue) => {
        try {
        setValue(newValue);
        localStorage.setItem(key, JSON.stringify(newValue));
        } catch (error) {
        console.error('localStorage error:', error);
        }
    }, [key]);

    const removeValue = useCallback(() => {
        try {
        setValue(initialValue);
        localStorage.removeItem(key);
        } catch (error) {
        console.error('localStorage error:', error);
        }
    }, [key, initialValue]);

    return [value, setStoredValue, removeValue];
    }

    // Usage
    function Settings() {
    const [theme, setTheme, removeTheme] = useLocalStorage('theme', 'light');

    return (
        <div>
        <p>Current theme: {theme}</p>
        <button onClick={() => setTheme('dark')}>Dark</button>
        <button onClick={() => setTheme('light')}>Light</button>
        <button onClick={removeTheme}>Reset</button>
        </div>
    );
    }
    ```

    ---

    **C3. Build a Debounced Search with React:**
    ```jsx
    function useDebounce(value, delay) {
    const [debouncedValue, setDebouncedValue] = useState(value);

    useEffect(() => {
        const timer = setTimeout(() => {
        setDebouncedValue(value);
        }, delay);

        return () => clearTimeout(timer);
    }, [value, delay]);

    return debouncedValue;
    }

    function SearchBar() {
    const [query, setQuery] = useState('');
    const [results, setResults] = useState([]);
    const [loading, setLoading] = useState(false);

    const debouncedQuery = useDebounce(query, 300);

    useEffect(() => {
        if (!debouncedQuery) {
        setResults([]);
        return;
        }

        setLoading(true);
        fetch(`/api/search?q=${debouncedQuery}`)
        .then(res => res.json())
        .then(data => {
            setResults(data);
            setLoading(false);
        });
    }, [debouncedQuery]); // only fires when debounced value changes!

    return (
        <div>
        <input
            value={query}
            onChange={e => setQuery(e.target.value)}
            placeholder="Search..."
        />
        {loading && <Spinner />}
        {results.map(item => (
            <div key={item.id}>{item.name}</div>
        ))}
        </div>
    );
    }
    ```

    ---

    **C4. Build a useReducer shopping cart:**
    ```jsx
    const initialState = { items: [], total: 0 };

    function cartReducer(state, action) {
    switch (action.type) {
        case 'ADD_ITEM':
        const existing = state.items.find(i => i.id === action.payload.id);
        if (existing) {
            return {
            ...state,
            items: state.items.map(i =>
                i.id === action.payload.id
                ? { ...i, quantity: i.quantity + 1 }
                : i
            ),
            total: state.total + action.payload.price
            };
        }
        return {
            ...state,
            items: [...state.items, { ...action.payload, quantity: 1 }],
            total: state.total + action.payload.price
        };

        case 'REMOVE_ITEM':
        const item = state.items.find(i => i.id === action.payload);
        return {
            ...state,
            items: state.items.filter(i => i.id !== action.payload),
            total: state.total - (item.price * item.quantity)
        };

        case 'CLEAR_CART':
        return initialState;

        default:
        return state;
    }
    }

    function ShoppingCart() {
    const [state, dispatch] = useReducer(cartReducer, initialState);

    return (
        <div>
        <p>Total: ${state.total}</p>
        {state.items.map(item => (
            <div key={item.id}>
            {item.name} x{item.quantity}
            <button onClick={() =>
                dispatch({ type: 'REMOVE_ITEM', payload: item.id })
            }>
                Remove
            </button>
            </div>
        ))}
        <button onClick={() => dispatch({ type: 'CLEAR_CART' })}>
            Clear Cart
        </button>
        </div>
    );
    }
    ```

    ---

    **C5. Build a Protected Route:**
    ```jsx
    function ProtectedRoute({ children }) {
    const { user, loading } = useAuth(); // custom auth hook
    const location = useLocation();

    if (loading) return <Spinner />;

    if (!user) {
        // Redirect to login, save current location
        return <Navigate to="/login" state={{ from: location }} replace />;
    }

    return children;
    }

    // Usage in router
    function App() {
    return (
        <Routes>
        <Route path="/login" element={<Login />} />
        <Route path="/public" element={<PublicPage />} />
        <Route
            path="/dashboard"
            element={
            <ProtectedRoute>
                <Dashboard />
            </ProtectedRoute>
            }
        />
        <Route
            path="/admin"
            element={
            <ProtectedRoute requiredRole="admin">
                <AdminPanel />
            </ProtectedRoute>
            }
        />
        </Routes>
    );
    }
    ```

    ---

    **C6. Build a Higher Order Component (HOC):**
    ```jsx
    // HOC — adds functionality to any component
    function withLoading(WrappedComponent) {
    return function WithLoadingComponent({ isLoading, ...props }) {
        if (isLoading) return <Spinner />;
        return <WrappedComponent {...props} />;
    };
    }

    function withAuth(WrappedComponent) {
    return function WithAuthComponent(props) {
        const { user } = useAuth();
        if (!user) return <Navigate to="/login" />;
        return <WrappedComponent {...props} user={user} />;
    };
    }

    // Usage
    const UserListWithLoading = withLoading(UserList);
    const ProtectedDashboard = withAuth(Dashboard);

    // In component
    <UserListWithLoading
    isLoading={loading}
    users={users}
    />
    ```

    ---

    **C7. Implement Pagination:**
    ```jsx
    function usePagination(data, itemsPerPage) {
    const [currentPage, setCurrentPage] = useState(1);

    const totalPages = Math.ceil(data.length / itemsPerPage);

    const currentData = useMemo(() => {
        const start = (currentPage - 1) * itemsPerPage;
        const end = start + itemsPerPage;
        return data.slice(start, end);
    }, [data, currentPage, itemsPerPage]);

    const goToPage = useCallback((page) => {
        setCurrentPage(Math.min(Math.max(1, page), totalPages));
    }, [totalPages]);

    return {
        currentData,
        currentPage,
        totalPages,
        goToPage,
        hasNext: currentPage < totalPages,
        hasPrev: currentPage > 1
    };
    }

    // Usage
    function UserTable({ users }) {
    const {
        currentData,
        currentPage,
        totalPages,
        goToPage,
        hasNext,
        hasPrev
    } = usePagination(users, 10);

    return (
        <div>
        {currentData.map(user => (
            <UserRow key={user.id} user={user} />
        ))}
        <div>
            <button onClick={() => goToPage(currentPage - 1)} disabled={!hasPrev}>
            Previous
            </button>
            <span>{currentPage} of {totalPages}</span>
            <button onClick={() => goToPage(currentPage + 1)} disabled={!hasNext}>
            Next
            </button>
        </div>
        </div>
    );
    }
    ```

    ---

    **C8. Build a Form with validation:**
    ```jsx
    function useForm(initialValues, validate) {
    const [values, setValues] = useState(initialValues);
    const [errors, setErrors] = useState({});
    const [touched, setTouched] = useState({});
    const [isSubmitting, setIsSubmitting] = useState(false);

    const handleChange = useCallback((e) => {
        const { name, value } = e.target;
        setValues(prev => ({ ...prev, [name]: value }));
        // Validate on change
        const fieldError = validate({ ...values, [name]: value });
        setErrors(prev => ({ ...prev, [name]: fieldError[name] }));
    }, [values, validate]);

    const handleBlur = useCallback((e) => {
        setTouched(prev => ({ ...prev, [e.target.name]: true }));
    }, []);

    const handleSubmit = useCallback((onSubmit) => async (e) => {
        e.preventDefault();
        const validationErrors = validate(values);
        setErrors(validationErrors);
        setTouched(Object.keys(values).reduce((acc, key) => ({
        ...acc, [key]: true
        }), {}));

        if (Object.keys(validationErrors).length === 0) {
        setIsSubmitting(true);
        await onSubmit(values);
        setIsSubmitting(false);
        }
    }, [values, validate]);

    return {
        values,
        errors,
        touched,
        isSubmitting,
        handleChange,
        handleBlur,
        handleSubmit
    };
    }

    // Usage
    function LoginForm() {
    const validate = (values) => {
        const errors = {};
        if (!values.email) errors.email = 'Email required';
        else if (!/\S+@\S+\.\S+/.test(values.email)) {
        errors.email = 'Invalid email';
        }
        if (!values.password) errors.password = 'Password required';
        else if (values.password.length < 6) {
        errors.password = 'Min 6 characters';
        }
        return errors;
    };

    const {
        values, errors, touched,
        handleChange, handleBlur, handleSubmit, isSubmitting
    } = useForm({ email: '', password: '' }, validate);

    return (
        <form onSubmit={handleSubmit(async (data) => {
        await loginAPI(data);
        })}>
        <input
            name="email"
            value={values.email}
            onChange={handleChange}
            onBlur={handleBlur}
        />
        {touched.email && errors.email && (
            <span className="error">{errors.email}</span>
        )}
        <input
            type="password"
            name="password"
            value={values.password}
            onChange={handleChange}
            onBlur={handleBlur}
        />
        {touched.password && errors.password && (
            <span className="error">{errors.password}</span>
        )}
        <button type="submit" disabled={isSubmitting}>
            {isSubmitting ? 'Loading...' : 'Login'}
        </button>
        </form>
    );
    }
    ```

    ---

    **C9. Implement a Toast Notification System:**
    ```jsx
    // Toast Context
    const ToastContext = createContext(null);

    function ToastProvider({ children }) {
    const [toasts, setToasts] = useState([]);

    const addToast = useCallback((message, type = 'info', duration = 3000) => {
        const id = Date.now();
        setToasts(prev => [...prev, { id, message, type }]);

        setTimeout(() => {
        setToasts(prev => prev.filter(t => t.id !== id));
        }, duration);
    }, []);

    const removeToast = useCallback((id) => {
        setToasts(prev => prev.filter(t => t.id !== id));
    }, []);

    return (
        <ToastContext.Provider value={{ addToast }}>
        {children}
        <div className="toast-container">
            {toasts.map(toast => (
            <div key={toast.id} className={`toast toast-${toast.type}`}>
                {toast.message}
                <button onClick={() => removeToast(toast.id)}>×</button>
            </div>
            ))}
        </div>
        </ToastContext.Provider>
    );
    }

    // Custom hook
    function useToast() {
    return useContext(ToastContext);
    }

    // Usage anywhere in app
    function MyComponent() {
    const { addToast } = useToast();

    return (
        <button onClick={() => addToast('Saved successfully!', 'success')}>
        Save
        </button>
    );
    }
    ```

    ---

    **C10. Implement React Query style data fetching:**
    ```jsx
    function useQuery(key, fetchFn, options = {}) {
    const {
        enabled = true,
        staleTime = 0,
        retry = 3,
        onSuccess,
        onError
    } = options;

    const [state, setState] = useState({
        data: null,
        loading: enabled,
        error: null,
        lastFetched: null
    });

    const retryCount = useRef(0);

    const fetchData = useCallback(async () => {
        setState(prev => ({ ...prev, loading: true, error: null }));

        try {
        const data = await fetchFn();
        setState({
            data,
            loading: false,
            error: null,
            lastFetched: Date.now()
        });
        onSuccess?.(data);
        retryCount.current = 0;
        } catch (error) {
        if (retryCount.current < retry) {
            retryCount.current++;
            setTimeout(fetchData, 1000 * retryCount.current);
        } else {
            setState(prev => ({
            ...prev,
            loading: false,
            error: error.message
            }));
            onError?.(error);
        }
        }
    }, [fetchFn, retry, onSuccess, onError]);

    useEffect(() => {
        if (!enabled) return;

        const isStale = !state.lastFetched ||
        Date.now() - state.lastFetched > staleTime;

        if (isStale) fetchData();
    }, [key, enabled]);

    return { ...state, refetch: fetchData };
    }

    // Usage
    function UserProfile({ userId }) {
    const { data: user, loading, error, refetch } = useQuery(
        `user-${userId}`,
        () => fetch(`/api/users/${userId}`).then(r => r.json()),
        {
        staleTime: 5 * 60 * 1000, // 5 minutes
        onSuccess: (data) => console.log('User loaded:', data),
        onError: (err) => console.error('Failed:', err)
        }
    );

    if (loading) return <Spinner />;
    if (error) return <Error message={error} onRetry={refetch} />;
    return <div>{user.name}</div>;
    }
    ```

    ---
--------------------------------------------------------------------------------------------------
## 🎯 SCENARIO QUESTIONS

    ---

    **S1. Your React app is very slow. Components are re-rendering too many times. How do you debug and fix it?**

    **Answer:**

    **Step 1 — Debug:**
    - Open React DevTools → Profiler tab
    - Click record → interact with app → stop recording
    - Look for components that render too often
    - Check render reasons (props/state changed?)

    **Step 2 — Fix:**
    ```jsx
    // Problem — parent re-render causes ALL children to re-render
    function Parent() {
    const [count, setCount] = useState(0);
    const [name, setName] = useState('');

    const handleClick = () => console.log('clicked'); // new function every render!

    return (
        <>
        <input value={name} onChange={e => setName(e.target.value)} />
        <ExpensiveChild onClick={handleClick} data={someData} />
        </>
    );
    }

    // Fixed
    function Parent() {
    const [count, setCount] = useState(0);
    const [name, setName] = useState('');

    const handleClick = useCallback(() => {
        console.log('clicked');
    }, []); // stable reference!

    const processedData = useMemo(() =>
        expensiveProcess(someData), [someData]
    ); // memoized calculation!

    return (
        <>
        <input value={name} onChange={e => setName(e.target.value)} />
        <ExpensiveChild onClick={handleClick} data={processedData} />
        </>
    );
    }

    const ExpensiveChild = React.memo(({ onClick, data }) => {
    // only re-renders if onClick or data actually changes!
    return <div>{/* expensive render */}</div>;
    });
    ```

    ---

    **S2. You need to share user authentication state across 50+ components. How do you architect this?**

    **Answer:**

    ```jsx
    // Architecture: Auth Context + Custom Hook

    // 1. Create Auth Context with all auth logic
    const AuthContext = createContext(null);

    function AuthProvider({ children }) {
    const [user, setUser] = useState(null);
    const [loading, setLoading] = useState(true);

    useEffect(() => {
        // Check for existing session on mount
        const token = localStorage.getItem('token');
        if (token) {
        validateToken(token)
            .then(user => setUser(user))
            .finally(() => setLoading(false));
        } else {
        setLoading(false);
        }
    }, []);

    const login = async (credentials) => {
        const { user, token } = await loginAPI(credentials);
        localStorage.setItem('token', token);
        setUser(user);
    };

    const logout = () => {
        localStorage.removeItem('token');
        setUser(null);
    };

    return (
        <AuthContext.Provider value={{ user, loading, login, logout }}>
        {children}
        </AuthContext.Provider>
    );
    }

    // 2. Custom hook — easy to use anywhere
    function useAuth() {
    const context = useContext(AuthContext);
    if (!context) throw new Error('useAuth must be inside AuthProvider');
    return context;
    }

    // 3. Use in any component
    function Header() {
    const { user, logout } = useAuth();
    return <div>Welcome {user?.name} <button onClick={logout}>Logout</button></div>;
    }
    ```

    ---

    **S3. Client wants real-time notifications in React app. How do you implement it?**

    **Answer:**

    ```jsx
    function useWebSocket(url) {
    const [messages, setMessages] = useState([]);
    const [connected, setConnected] = useState(false);
    const wsRef = useRef(null);

    useEffect(() => {
        wsRef.current = new WebSocket(url);

        wsRef.current.onopen = () => setConnected(true);

        wsRef.current.onmessage = (event) => {
        const data = JSON.parse(event.data);
        setMessages(prev => [...prev, data]);
        };

        wsRef.current.onclose = () => {
        setConnected(false);
        // Reconnect after 3 seconds
        setTimeout(() => {
            wsRef.current = new WebSocket(url);
        }, 3000);
        };

        return () => wsRef.current?.close(); // cleanup!
    }, [url]);

    const sendMessage = useCallback((message) => {
        if (wsRef.current?.readyState === WebSocket.OPEN) {
        wsRef.current.send(JSON.stringify(message));
        }
    }, []);

    return { messages, connected, sendMessage };
    }

    // Usage
    function NotificationPanel({ userId }) {
    const { messages, connected } = useWebSocket(
        `wss://api.example.com/notifications/${userId}`
    );

    return (
        <div>
        <span>{connected ? '🟢 Live' : '🔴 Reconnecting...'}</span>
        {messages.map(msg => (
            <Notification key={msg.id} message={msg} />
        ))}
        </div>
    );
    }
    ```

    ---

    **S4. You need to build a config-driven UI where UI changes based on JSON config from backend. How do you design this in React?**

    **Answer:** (This is exactly what you did in your project!)

    ```jsx
    // Backend sends config like this:
    const uiConfig = {
    sections: [
        {
        type: "form",
        id: "user-form",
        fields: [
            { type: "text", name: "firstName", label: "First Name", required: true },
            { type: "email", name: "email", label: "Email", required: true },
            { type: "select", name: "role", label: "Role",
            options: ["Admin", "User", "Viewer"] }
        ]
        },
        {
        type: "table",
        id: "users-table",
        columns: ["Name", "Email", "Role", "Actions"]
        },
        {
        type: "chart",
        id: "analytics-chart",
        chartType: "bar",
        dataKey: "monthly-users"
        }
    ]
    };

    // Component registry — maps type to component
    const COMPONENT_MAP = {
    form: DynamicForm,
    table: DynamicTable,
    chart: DynamicChart,
    text: TextInput,
    email: EmailInput,
    select: SelectInput
    };

    // Config renderer — renders any config
    function ConfigRenderer({ config }) {
    return (
        <div>
        {config.sections.map(section => {
            const Component = COMPONENT_MAP[section.type];
            if (!Component) return null;
            return <Component key={section.id} config={section} />;
        })}
        </div>
    );
    }

    // Dynamic form builder
    function DynamicForm({ config }) {
    const [values, setValues] = useState({});

    return (
        <form>
        {config.fields.map(field => {
            const FieldComponent = COMPONENT_MAP[field.type];
            return (
            <FieldComponent
                key={field.name}
                config={field}
                value={values[field.name] || ''}
                onChange={(value) => setValues(prev => ({
                ...prev,
                [field.name]: value
                }))}
            />
            );
        })}
        </form>
    );
    }
    ```

    ---

    **S5. Your app loads slowly — initial bundle is 5MB. How do you optimize it?**

    **Answer:**

    ```jsx
    // 1. Code splitting — split by route
    import { lazy, Suspense } from 'react';

    // Before — ALL components loaded upfront
    import Dashboard from './Dashboard'; // 500KB
    import Analytics from './Analytics'; // 800KB
    import Reports from './Reports';     // 1MB

    // After — load only when route is visited
    const Dashboard = lazy(() => import('./Dashboard'));
    const Analytics = lazy(() => import('./Analytics'));
    const Reports = lazy(() => import('./Reports'));

    function App() {
    return (
        <Suspense fallback={<PageLoader />}>
        <Routes>
            <Route path="/dashboard" element={<Dashboard />} />
            <Route path="/analytics" element={<Analytics />} />
            <Route path="/reports" element={<Reports />} />
        </Routes>
        </Suspense>
    );
    }

    // 2. Dynamic imports for heavy libraries
    // Before
    import * as Plotly from 'plotly.js'; // 3MB loaded immediately!

    // After — load only when chart renders
    async function loadChart(data) {
    const Plotly = await import('plotly.js');
    Plotly.newPlot('chart', data);
    }

    // 3. Tree shaking — import only what you need
    // Before
    import _ from 'lodash'; // entire 72KB lodash!
    // After
    import debounce from 'lodash/debounce'; // just 2KB!

    // 4. Analyze bundle size
    // Add to package.json scripts:
    // "analyze": "source-map-explorer 'build/static/js/*.js'"
    ```

    ---

    **S6. How do you handle race conditions in React data fetching?**

    **Answer:**

    ```jsx
    // Problem — race condition!
    function UserProfile({ userId }) {
    const [user, setUser] = useState(null);

    useEffect(() => {
        fetch(`/api/users/${userId}`)
        .then(r => r.json())
        .then(data => setUser(data)); // might set stale data!
    }, [userId]);
    // If userId changes quickly (1→2→3),
    // response for userId=1 might arrive AFTER userId=3
    // Setting stale data from userId=1!
    }

    // Fix — cleanup flag OR AbortController
    function UserProfile({ userId }) {
    const [user, setUser] = useState(null);

    useEffect(() => {
        let isCurrent = true; // flag for this specific effect run

        fetch(`/api/users/${userId}`)
        .then(r => r.json())
        .then(data => {
            if (isCurrent) setUser(data); // only set if still current!
        });

        return () => { isCurrent = false; }; // cancel on next effect
    }, [userId]);
    }

    // Even better — AbortController
    function UserProfile({ userId }) {
    const [user, setUser] = useState(null);

    useEffect(() => {
        const controller = new AbortController();

        fetch(`/api/users/${userId}`, { signal: controller.signal })
        .then(r => r.json())
        .then(data => setUser(data))
        .catch(err => {
            if (err.name !== 'AbortError') console.error(err);
        });

        return () => controller.abort(); // cancel pending request!
    }, [userId]);
    }
    ```

    ---

    **S7. How do you implement dark/light theme in a large React application?**

    **Answer:**

    ```jsx
    // 1. Create theme context
    const ThemeContext = createContext(null);

    // 2. Theme provider with persistence
    function ThemeProvider({ children }) {
    const [theme, setTheme] = useLocalStorage('theme', 'light');

    useEffect(() => {
        // Apply theme to root element
        document.documentElement.setAttribute('data-theme', theme);
        // OR add/remove class
        document.body.classList.toggle('dark-mode', theme === 'dark');
    }, [theme]);

    const toggleTheme = useCallback(() => {
        setTheme(prev => prev === 'light' ? 'dark' : 'light');
    }, [setTheme]);

    return (
        <ThemeContext.Provider value={{ theme, toggleTheme }}>
        {children}
        </ThemeContext.Provider>
    );
    }

    // 3. CSS variables for theming
    /* index.css */
    /*
    :root[data-theme="light"] {
    --bg-primary: #ffffff;
    --text-primary: #000000;
    --accent: #3498db;
    }

    :root[data-theme="dark"] {
    --bg-primary: #1a1a1a;
    --text-primary: #ffffff;
    --accent: #5dade2;
    }
    */

    // 4. Components use CSS variables automatically!
    // .button { background: var(--accent); color: var(--text-primary); }

    // 5. Theme toggle button anywhere
    function ThemeToggle() {
    const { theme, toggleTheme } = useContext(ThemeContext);
    return (
        <button onClick={toggleTheme}>
        {theme === 'light' ? '🌙 Dark' : '☀️ Light'}
        </button>
    );
    }
    ```

    ---

    **S8. How do you test a React component that fetches data? Explain your approach.**

    **Answer:**

    ```jsx
    // Component to test
    function UserList() {
    const { data: users, loading, error } = useFetch('/api/users');

    if (loading) return <div>Loading...</div>;
    if (error) return <div>Error: {error}</div>;
    return (
        <ul>
        {users.map(user => (
            <li key={user.id}>{user.name}</li>
        ))}
        </ul>
    );
    }

    // Test file — UserList.test.jsx
    import { render, screen, waitFor } from '@testing-library/react';

    // Mock fetch globally
    global.fetch = jest.fn();

    describe('UserList', () => {
    beforeEach(() => {
        fetch.mockClear();
    });

    test('shows loading initially', () => {
        fetch.mockResolvedValueOnce({
        ok: true,
        json: async () => []
        });

        render(<UserList />);
        expect(screen.getByText('Loading...')).toBeInTheDocument();
    });

    test('renders users after fetch', async () => {
        const mockUsers = [
        { id: 1, name: 'Sakthi' },
        { id: 2, name: 'Kumar' }
        ];

        fetch.mockResolvedValueOnce({
        ok: true,
        json: async () => mockUsers
        });

        render(<UserList />);

        await waitFor(() => {
        expect(screen.getByText('Sakthi')).toBeInTheDocument();
        expect(screen.getByText('Kumar')).toBeInTheDocument();
        });
    });

    test('shows error when fetch fails', async () => {
        fetch.mockRejectedValueOnce(new Error('Network error'));

        render(<UserList />);

        await waitFor(() => {
        expect(screen.getByText(/Error/)).toBeInTheDocument();
        });
    });
    });
    ```

    ---

    **S9. Senior/Lead Question: How do you architect a large React application with 100+ components?**

    **Answer:**

    **Folder Structure:**
    ```
    src/
    ├── components/          # Reusable UI components
    │   ├── Button/
    │   │   ├── Button.jsx
    │   │   ├── Button.test.jsx
    │   │   └── index.js
    │   └── Modal/
    ├── features/           # Feature-based modules
    │   ├── auth/
    │   │   ├── components/
    │   │   ├── hooks/
    │   │   ├── services/
    │   │   └── store/
    │   └── dashboard/
    ├── hooks/              # Shared custom hooks
    ├── services/           # API layer
    ├── store/              # Global state (Redux)
    ├── utils/              # Helper functions
    ├── constants/          # App constants
    └── pages/              # Route components
    ```

    **Key principles:**
    1. **Feature-based structure** — group by feature not file type
    2. **Barrel exports** — `index.js` for clean imports
    3. **Custom hooks** — extract all logic from components
    4. **Service layer** — all API calls in one place
    5. **Atomic Design** — atoms → molecules → organisms → templates → pages
    6. **Consistent naming** — PascalCase for components, camelCase for hooks
    7. **Shared component library** — reusable design system

    ---

    **S10. How do you handle micro-frontend architecture in React?**

    **Answer:** (You have real experience with this!)

    ```jsx
    // Module Federation (Webpack 5) — share components between apps

    // webpack.config.js (Host app)
    new ModuleFederationPlugin({
    name: 'host',
    remotes: {
        auth: 'auth@http://localhost:3001/remoteEntry.js',
        dashboard: 'dashboard@http://localhost:3002/remoteEntry.js',
        reports: 'reports@http://localhost:3003/remoteEntry.js',
    },
    shared: {
        react: { singleton: true, requiredVersion: '^18.0.0' },
        'react-dom': { singleton: true }
    }
    });

    // webpack.config.js (Remote app — auth)
    new ModuleFederationPlugin({
    name: 'auth',
    filename: 'remoteEntry.js',
    exposes: {
        './LoginPage': './src/pages/Login',
        './useAuth': './src/hooks/useAuth',
    },
    shared: { react: { singleton: true } }
    });

    // In host app — load remote components
    const LoginPage = lazy(() => import('auth/LoginPage'));
    const Dashboard = lazy(() => import('dashboard/Dashboard'));

    function App() {
    return (
        <Suspense fallback={<PageLoader />}>
        <Routes>
            <Route path="/login" element={<LoginPage />} />
            <Route path="/dashboard" element={<Dashboard />} />
        </Routes>
        </Suspense>
    );
    }

    // Benefits:
    // - Independent deployments per team
    // - Shared dependencies (React loaded once!)
    // - Teams work independently
    // - Gradual migration possible
    ```
--------------------------------------------------------------------------------------------------

    ---

    ## ✅ REACT COMPLETE SUMMARY

    | Section | Count |
    |---------|-------|
    | 🗺️ Mind Map | 8 categories |
    | 📖 Theory Questions | 10 ✅ |
    | 💻 Coding Questions | 10 ✅ |
    | 🎯 Scenario Questions | 10 ✅ |

    **Total: 30 React Questions — Complete!** 🎉

    ---

    ### 🏆 Topics Covered:
    ✅ Virtual DOM & JSX
    ✅ Functional vs Class Components
    ✅ Props vs State
    ✅ All Hooks (useState, useEffect, useRef, useContext, useReducer, useMemo, useCallback)
    ✅ Custom Hooks
    ✅ Context API
    ✅ Redux & Redux Toolkit
    ✅ Performance Optimization
    ✅ HOC, Error Boundaries, Portals, forwardRef
    ✅ React Router & Protected Routes
    ✅ Real world scenarios (Config-driven UI, Micro-frontends, WebSockets)
    ✅ Testing

    ---

    Ready to move to **Backend (Node.js + Express) Deep Dive** with same format? 🚀😊